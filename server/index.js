require('dotenv').config();const express=require('express');const cors=require('cors');const morgan=require('morgan');const auth=require('./routes/auth');const tracks=require('./routes/tracks');const payments=require('./routes/payments');const app=express();app.use(cors({origin:process.env.FRONTEND_URL||'*'}));app.use(express.json());app.use(morgan('tiny'));app.use('/api/auth',auth);app.use('/api/tracks',tracks);app.use('/api/payments',payments);app.get('/',(req,res)=>res.send('MusixOne backend'));const PORT=process.env.PORT||4000;app.listen(PORT,()=>console.log('MusixOne backend listening on',PORT));